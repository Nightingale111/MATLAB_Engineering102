% expression_evaluator: evaluates four different expressions

A = 1;
B = 1.5;
C = -10;
D = 2.5;

% Problem A
disp('4B + C = ');
disp((4*D)+C);

% Problem B
disp('D^2 + 1.5D - 10 = ');
disp((D^2)+(1.5*D)-10);

% Problem C
disp('(-B + sqrt(B^2 - 4AC)) / (2A) = ');
disp((-B + sqrt(B^2 - 4*ac0Reader)) / (2A))